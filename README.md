# Bankist App 🏦
A sleek, functional banking application built with Vanilla JavaScript. This project focuses on DOM manipulation, user authentication simulation, and complex data management using modern JavaScript array methods. It features real-time balance updates, money transfers, loan requests, and a dynamic transaction history.
Bankist is a minimalist banking application that allows users to manage their finances through a clean, interactive interface. This project was developed to practice advanced JavaScript concepts and DOM manipulation.

## 🚀 Live Demo
[Insert your GitHub Pages link or Vercel link here]

## ✨ Key Features
* **User Authentication:** Secure-style login (using credentials like `js` and `1111`).
* **Transaction History:** Dynamic list of deposits and withdrawals with formatted dates and currency.
* **Financial Summary:** Real-time calculation of total balance, incomes, outgoings, and interest.
* **Money Transfers:** Functionality to transfer funds between different accounts with validation.
* **Loan Requests:** A rule-based loan system (requires at least one deposit ≥ 10% of the loan amount).
* **Account Management:** Ability to close accounts.
* **Sorting:** Sort transactions by amount.
* **Auto-Logout:** A security timer that logs users out after 5 minutes of inactivity.

## 🛠️ Tech Stack
* **HTML5 & CSS3:** Flexbox and CSS Grid for a responsive, modern layout.
* **Vanilla JavaScript:** Using the "State" management pattern without external frameworks.
* **Intl API:** For localized currency formatting and date display (supports EUR, USD, GBP, etc.).

## 🧠 Technical Highlights
* **Advanced Array Methods:** Extensive use of `map`, `filter`, `reduce`, `find`, and `flatMap` to process account data.
* **Timers:** Implementation of `setInterval` and `setTimeout` for the session logout feature.
* **DOM Manipulation:** Efficiently updating the UI using `insertAdjacentHTML` and changing styles dynamically.
* **Internationalization:** Handling different locales and currencies based on the logged-in user's profile.

## 🔑 Demo Credentials
| User | PIN |
| :--- | :--- |
| John Doe (`jd`) | 1111 |
| Jessica Davis (`jd`) | 2222 |
| Steven Thomas Williams (`stw`) | 3333 |

---
*Note: This project was built as part of the "The Complete JavaScript Course" by Jonas Schmedtmann.*
